const panelNoText = document.getElementById("panel-no-text")
const textoIngresado = document.getElementById("texto-usuario")
const textoResultado = document.getElementById("resultado")
const contenedorResult = document.getElementById("resp")

const draw = document.getElementById("draw")
const ningunMensaje = document.getElementById("no-text")
const message = document.getElementById("message") 

export {
    panelNoText,
    textoIngresado,
    textoResultado,
    contenedorResult,
    message,
    draw,
    ningunMensaje
}